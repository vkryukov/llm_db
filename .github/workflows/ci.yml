# Standardized CI workflow using central reusable workflows
# See: https://github.com/agentjido/github-actions 

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    uses: agentjido/github-actions/.github/workflows/elixir-lint.yml@v1

  test:
    name: Test
    uses: agentjido/github-actions/.github/workflows/elixir-test.yml@v1
